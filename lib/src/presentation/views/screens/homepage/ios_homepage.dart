import 'package:flutter/material.dart';

class IosHomePage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('welcome to homepage from Ios'),
      ),
    );
  }
}